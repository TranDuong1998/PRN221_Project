@page
@model PRN211_Project.Pages.Slots.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1 class=" text-white">TimeSlot Management</h1>

<div class="container bg-light text-black rounded">
    <div class="container py-2">
        <h4 class="mt-3">Update TimeSlot</h4>
        <hr />
        <div class="row">
            <div class="col-md-4">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="TimeSlot.TimeSlotId" />
                    <div class="form-group mb-2">
                        <label asp-for="TimeSlot.StartTime" class="control-label">Start Time</label>
                        <input asp-for="TimeSlot.StartTime" class="form-control" type="time" />
                        <span asp-validation-for="TimeSlot.StartTime" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-2">
                        <label asp-for="TimeSlot.EndTime" class="control-label">End Time</label>
                        <input asp-for="TimeSlot.EndTime" class="form-control" type="time" />
                        <span asp-validation-for="TimeSlot.EndTime" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="TimeSlot.Description" class="control-label"></label>
                        <input asp-for="TimeSlot.Description" class="form-control"/>
                        <span asp-validation-for="TimeSlot.Description" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-2">
                        <input type="submit" value="Save" class="btn btn-primary" />
                    </div>
                </form>
            </div>
            <div class="mb-2">
                <a asp-page="./Index"><i class="fas fa-arrow-left"></i> Back to List</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
